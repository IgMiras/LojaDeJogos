import { Layout } from "@/components/Layout";
import { TodosJogos } from "@/components/ListarTodosJogos";

export default function ListagemDeTodosJogos() {
  return (
    <Layout>
      <TodosJogos />
    </Layout>
  );
}
